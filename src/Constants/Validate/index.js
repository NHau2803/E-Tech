exports.VALIDATE_MESSAGES = {
    required: "Đang trống!",
    types: {
        email: "${label} chưa là là email!",
        number: "${label} chưa là số!"
    },
    number: {
        range: "${label} chỉ cho phép từ ${min} đến ${max}"
    }
}
